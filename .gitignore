# Rust
/target
**/target

# Node / frontend build
node_modules
frontend/build
frontend/.svelte-kit
frontend/package

# Tauri
src-tauri/gen/schemas

# Local encrypted database — never commit user financial data
*.db
*.db-*
*.sqlite
*.sqlite-*

# Real bank export(s) used for manual CSV-import testing — never commit
# real personal financial data, even for test fixtures.
/sample-data
T_cpte_*.csv

# OS / editor
.DS_Store
.env
.env.*
!.env.example
